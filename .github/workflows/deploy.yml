name: Deploy Nubo Ktor API to Amazon EC2

on:
  push:
    branches:
      - master
      - develop
  workflow_dispatch:
    inputs:
      branch:
        description: 'Rama a desplegar'
        required: true
        default: 'develop'
        type: choice
        options:
          - master
          - develop

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch || github.ref }}

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'gradle'

      - name: Make Gradle wrapper executable
        run: chmod +x gradlew

      - name: Build with Gradle and create fat JAR
        run: ./gradlew clean shadowJar

      - name: List generated JARs
        run: find build/libs -name "*.jar" -print

      - name: Move JAR to root and prepare for deployment
        id: move_jar
        run: |
          JAR_FULL_PATH=$(find build/libs -name "*-all.jar" | head -n 1)
          if [ -z "$JAR_FULL_PATH" ]; then
            echo "Error: No se encontró el JAR generado"
            exit 1
          fi
          JAR_FILENAME=$(basename "$JAR_FULL_PATH")
          # Renombramos el JAR a un nombre genérico para la copia
          mv "$JAR_FULL_PATH" "nubo-api.jar"
          echo "JAR movido y renombrado a: nubo-api.jar"
          echo "moved_jar_filename=nubo-api.jar" >> $GITHUB_OUTPUT

      # -------------------------------------------------------------------
      # CAMBIO 1: RUTA Y USUARIO PARA PRE-DEPLOYMENT
      # -------------------------------------------------------------------
      - name: Execute pre-deployment script on EC2 (Stop old app)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
          script: |
            sudo /opt/NuboAPI/scripts/pre_deploy

      # -------------------------------------------------------------------
      # CAMBIO 2: RUTA DE DESTINO PARA SCP (COPIA DEL JAR)
      # -------------------------------------------------------------------
      - name: Copy JAR to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
          source: "${{ steps.move_jar.outputs.moved_jar_filename }}"
          target: "/opt/NuboAPI"
          overwrite: true

      # -------------------------------------------------------------------
      # CAMBIO 3: RUTA Y USUARIO PARA POST-DEPLOYMENT
      # -------------------------------------------------------------------
      - name: Execute post-deployment script on EC2 (Start app)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
          script: |
            sudo /opt/NuboAPI/scripts/post_deploy

      # -------------------------------------------------------------------
      # CAMBIO 4: VERIFICACIÓN DEL SERVICIO SYSTEMD
      # -------------------------------------------------------------------
      - name: Verify deployment
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
          script: |
            echo "Verificando estado del servicio..."
            sudo systemctl status NubiAPI || echo "Servicio no configurado aún"
            echo "Verificando puerto 9000..."
            # Asumiendo que tu app usa el puerto 9000
            sudo netstat -tulpn | grep 9000 || echo "Puerto 9000 no está escuchando"